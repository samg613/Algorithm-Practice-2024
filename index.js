function binarytoDecimal(math) {
  let decimal = 0;
  for (i = 0; i < length; i++) {
    decimal += Number(c[length - 1 - i]) * Math.pow(2, i);

    return decimal;
  }
  console.log(binarytoDecimal(11));
}


//whalen func
function binarytoDecimal(binary) {
  let decimal = 0;
  for (i = 0; i < length; i++) {
  if (binary[binary.length - i- 1]) * Math.pow(2, i);

    return decimal;
  }
}
let x = "1011";
x[x.length - i - 1];
